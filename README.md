# Лабораторная работа №6: PHP, Docker и ClickHouse

![ClickHouse](https://img.shields.io/badge/ClickHouse-FFCC00?style=for-the-badge&logo=clickhouse&logoColor=black)
![Docker](https://img.shields.io/badge/docker-%230db7ed.svg?style=for-the-badge&logo=docker&logoColor=white)
![Composer](https://img.shields.io/badge/Composer-885610?style=for-the-badge&logo=composer&logoColor=white)
![PHP](https://img.shields.io/badge/php-%23777BB4.svg?style=for-the-badge&logo=php&logoColor=white)

## 👩‍💻 Автор
Чепурнова Аделина, 3МО-1

---

## 📌 Описание задания
Целью работы было изучение взаимодействия с нереляционной, поисковой базой данных **Elasticsearch** из PHP. Проект был полностью перестроен для использования профессионального подхода с кастомным **Dockerfile**, **PHP-пространствами имен (namespaces)** и **PSR-4 автозагрузкой** через Composer.

Результат доступен по адресу:
- Главная страница: [http://localhost:8080](http://localhost:8080)

---

## ⚙️ Как запустить проект

1.  Клонировать репозиторий:
    ```bash
    git clone https://github.com/4hurs/webdev_labs_5sem.git
    cd nginx-docker-lab 
    ```
2.  Запустить контейнеры с пересборкой PHP-образа:
    ```bash
    docker-compose up -d --build
    ```
3.  Открыть сайт в браузере. При первом запуске `index.php` автоматически создаст таблицу в  индекс в Elasticsearch и заполняется тестовыми данными блога.

---

## 📂 Новое в проекте

*   `Dockerfile` — Создает кастомный образ `php:8.2-fpm`, в который устанавливаются необходимые расширения и **Composer** со всеми зависимостями.
*   `docker-compose.yml` — Добавлен сервис для базы данных **Elasticsearch**. Сервис `php` теперь собирается из `Dockerfile`, а не использует готовый образ.
*   `composer.json` — Настроена **PSR-4 автозагрузка**, которая связывает пространство имен `App\` с директорией `www/App/`, позволяя автоматически подключать классы.
*   `www/App/` — Новая директория для классов приложения:
    *   `ClientFactory.php` — Фабрика для создания экземпляров Guzzle-клиента.
    *   `BlogManager.php` — Класс для работы с **Elasticsearch** (создание индекса, управление постами, поиск).
*   `www/index.php` — Главный скрипт с демонстрацией функционала блога.

---

## 📸 Скриншот выполнения

При первом запуске автоматически создается индекс в Elasticsearch и заполняется тестовыми данными блога.

![Результат](Лабораторная%20работа%20№6/screenshots/01.png)

---

## ✅ Результат

В ходе работы была успешно интегрирована аналитическая база данных **Elasticsearch** в Docker-окружение. Структура PHP-проекта была полностью реорганизована для использования **пространств имен** и **PSR-4 автозагрузки**, что является стандартом в современной веб-разработке. Взаимодействие с базой данных инкапсулировано в отдельный класс (`BlogManager`), который общается с HTTP-интерфейсом Elasticsearch через Guzzle. Это демонстрирует полный и современный workflow для создания PHP-приложений.
